name: "PR Explainer"
description: "Generates a personalized explainer for a merged PR and commits it."
author: "shilpi1958"
branding:
  icon: "book-open"
  color: "purple"

inputs:
  claude-code-oauth-token:
    description: >
      Long-lived token from a Claude subscription, created locally with
      `claude setup-token` and stored as a repo secret. Preferred — no
      separate API billing. Provide this or anthropic-api-key.
    required: false
  anthropic-api-key:
    description: "Anthropic Console API key. Used only if claude-code-oauth-token is not set."
    required: false
  posthog-api-key:
    description: >
      PostHog project API key (POSTHOG_API_KEY). Enables product analytics and
      $ai_generation capture for AI Evals. Optional — CLI works without it.
    required: false
  posthog-host:
    description: "PostHog host, e.g. https://us.i.posthog.com"
    required: false
    default: "https://us.i.posthog.com"
  profile-path:
    description: "Path to the learning profile file"
    required: false
    default: "learning-profile.md"
  output-dir:
    description: "Directory to write explainers into"
    required: false
    default: "docs/explainers"
  commit:
    description: "If true, commits and pushes the generated entry directly to the merge commit's branch"
    required: false
    default: "true"

runs:
  using: "composite"
  steps:
    - name: Set up Node
      uses: actions/setup-node@v4
      with:
        node-version: "20"

    - name: Install code-explainer and Claude Code CLI
      shell: bash
      run: |
        npm install --no-save @shilpi1958/code-explainer @anthropic-ai/claude-code

    - name: Generate explainer
      id: generate
      shell: bash
      env:
        CLAUDE_CODE_OAUTH_TOKEN: ${{ inputs.claude-code-oauth-token }}
        ANTHROPIC_API_KEY: ${{ inputs.anthropic-api-key }}
        LEARNING_PROFILE: ${{ inputs.profile-path }}
        EXPLAINER_DIR: ${{ inputs.output-dir }}
        GH_TOKEN: ${{ github.token }}
        POSTHOG_API_KEY: ${{ inputs.posthog-api-key }}
        POSTHOG_HOST: ${{ inputs.posthog-host }}
      run: |
        OUT_PATH=$(npx code-explainer pr "${{ github.event.pull_request.number }}")
        echo "path=$OUT_PATH" >> "$GITHUB_OUTPUT"

    - name: Commit entry
      if: ${{ inputs.commit == 'true' }}
      shell: bash
      run: |
        git config user.name "code-explainer[bot]"
        git config user.email "actions@github.com"
        git add "${{ steps.generate.outputs.path }}" "$(dirname "${{ steps.generate.outputs.path }}")/index.md"
        git commit -m "docs: explainer for #${{ github.event.pull_request.number }}"
        git push
